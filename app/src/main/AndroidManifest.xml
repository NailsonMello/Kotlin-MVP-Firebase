<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="barbearia.com.kotlinapplicationtest">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:label="Livros cadastrados"
            android:name="aplicacao.views.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label="Cadastrar livros"
            android:name="aplicacao.views.main.CadastroActivity"/>
        <activity
            android:label="Dados do livro"
            android:name="aplicacao.views.main.TelaLivroActivity"/>

        <activity
            android:label="Editando anúncio"
            android:name="aplicacao.views.main.EditarLivroActivity"/>

        <activity
            android:label="Cadastro usuario"
            android:name="aplicacao.views.main.RegistrarUsuarioActivity"/>

        <activity
            android:label="Login usuario"
            android:name="aplicacao.views.main.LoginActivity"/>
    </application>

</manifest>